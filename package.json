{
  "name": "electron",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager ./ HelloWorld --platform=win32 --arch=all --out ./outApp --electronVersion 1.0.1 --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "electron-builder --win --x64"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron-builder": "^20.39.0",
    "getmac": "^1.4.6"
  },
  "dependencies": {
    "getmac": "^1.4.6"
  }
}
